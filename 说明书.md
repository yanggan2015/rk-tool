# Rockchip Windows CLI 升级工具包

在 Windows 下用命令行烧写瑞芯微固件，**不依赖 RKDevTool GUI**。

支持两类烧写：

1. **整包**：`update.img`（`UF`）
2. **单分区**：`boot.img` / `uboot.img` / `rootfs.img` 等（`DI`）

## 目录说明

```text
rk-tool/
├── flash.bat                 ← 推荐入口（转调 scripts\flash.ps1）
├── README.md
├── 说明书.md                 ← 本说明书
├── firmware/                 ← 放置待烧镜像（*.img 不入库）
│   └── README.txt
├── scripts/
│   ├── flash.ps1             ← 主脚本（整包 + 单分区）
│   ├── flash.bat
│   ├── flash-update.bat      ← 兼容旧入口，等价于 flash.bat
│   ├── list-device.bat       ← 列出已连接设备
│   └── rk.bat                ← 透传任意 upgrade_tool 命令
└── tools/
    ├── upgrade_tool/         ← 官方 Windows CLI（v2.23）
    │   ├── upgrade_tool.exe
    │   ├── config.ini
    │   └── upgrade_tool_manual.pdf
    └── DriverAssistant/      ← USB 驱动安装器 v5.12
        └── DriverInstall.exe
```

工具来源：Vicharak 公开整理的 Rockchip 工具集  
`https://github.com/vicharak-in/rockchip-linux-tools`（`upgrade_tool_v2.23` / `DriverAssitant_v5.12`）

---

## 第一次使用：安装驱动

1. 以**管理员身份**运行：

   `tools\DriverAssistant\DriverInstall.exe`

2. 建议先点「驱动卸载」，再点「驱动安装」。

3. 用 USB 连接板子，让设备进入 **Loader** 或 **Maskrom** 模式。  
   设备管理器中应出现类似 **Rockusb Device** 的设备。

> 不同板子进模式方式不同：常见是按住 Recovery / Maskrom 键再上电，或以厂商文档为准。

检查设备：

```bat
flash.bat status
flash.bat maskrom
flash.bat ld
flash.bat info
```

- `status` / `ld`：列出设备、模式、**LocationID**（多设备时用 `-s`）
- `maskrom`：仅当当前是 Maskrom 时返回成功（exit 0）
- `wait-maskrom`：等待进入 Maskrom（可用 `-Timeout 120`）
- `info`：芯片 ID + 存储信息 + 分区表

---

## 多设备选择

手册：先 `LD` 列出设备，记下 `LocationID`，操作时加 `-s`。

```bat
flash.bat ld
flash.bat -s 244 status
flash.bat -s 244 update
flash.bat -s 244 info
flash.bat -s 244 erase
```

同时连接多台且未指定 `-s` 时，脚本会拒绝烧写/擦除，避免写错板。

---

## 读取设备信息

```bat
flash.bat info      芯片 ID (RCI) + 存储 (RFI) + 分区表 (PL)
flash.bat rci       仅芯片 ID
flash.bat rfi       仅存储信息
flash.bat pl        仅分区表
```

`rfi` / `pl` 在 Maskrom 下会先 Download Boot（需 MiniLoader）。

---

## 设备擦除

手册要求 **Maskrom** 下整片擦除，且不必先 DB：

```bat
flash.bat erase
flash.bat erase firmware\MiniLoaderAll.bin
flash.bat -s 244 erase
```

按扇区擦除（仅 eMMC）：

```bat
flash.bat el 0 0x2000
```

擦除会提示确认；自动化可加 `-SkipConfirm`。

---

## 下载 Boot / 切换模式 / 多存储

```bat
flash.bat db                 Maskrom 下载 Boot（不写存储）
flash.bat rd                 复位
flash.bat to-maskrom         Loader → Maskrom（rd 3）
flash.bat ssd                多存储切换（需已 DB）
flash.bat loader -Storage SPINOR
```

---

## 按地址读写

```bat
flash.bat wl 0x12000 oem.img
flash.bat rl 0x12000 0x2000 out.img
```

---

## 整包烧写 update.img

1. 把固件放到 `firmware\update.img`（推荐）。  
   若文件仍在工程根目录 `update.img`，脚本同样能找到。

2. 设备进入 Loader / Maskrom 后运行：

   ```bat
   flash.bat
   ```

   等价写法：

   ```bat
   flash.bat update
   flash.bat firmware\update.img
   scripts\flash-update.bat
   ```

3. 看到设备列表后，输入 `Y` 确认，等待 `Upgrade firmware ok`。

也可直接指定路径：

```bat
flash.bat D:\images\rk3588_update.img
```

可选参数：

```bat
flash.bat update -EraseFirst
flash.bat update -NoReset -SkipConfirm
```

Maskrom 下烧整包 **不需要** 单独的 MiniLoader：`update.img` 内已包含 loader，`UF` 会自行完成。

---

## 单烧分区镜像

把对应文件放到 `firmware\`，文件名需能识别分区：

| 命令 | 默认文件 | upgrade_tool |
|------|----------|--------------|
| `flash.bat boot` | `firmware\boot.img` | `DI -boot` |
| `flash.bat uboot` | `firmware\uboot.img` | `DI -uboot` |
| `flash.bat rootfs` | `firmware\rootfs.img` | `DI -rootfs` |
| `flash.bat recovery` | `firmware\recovery.img` | `DI -recovery` |
| `flash.bat misc` | `firmware\misc.img` | `DI -misc` |
| `flash.bat oem` | `firmware\oem.img` | `DI -oem` |
| `flash.bat userdata` | `firmware\userdata.img` | `DI -userdata` |
| `flash.bat loader` | `firmware\MiniLoaderAll.bin` | `UL` |

一次烧多个分区（只确认一次）：

```bat
flash.bat boot uboot rootfs
```

指定任意路径：

```bat
flash.bat D:\images\boot.img
flash.bat -Partition rootfs D:\images\my-rootfs.img
```

查看当前设备分区表（需已在 Loader 模式）：

```bat
flash.bat pl
```

### Maskrom 下单分区

设备若处于 **Maskrom**，单分区烧写前必须先 Download Boot。请把 MiniLoader 放到 `firmware\`，常见文件名：

- `MiniLoaderAll.bin`
- `rk3588_spl_loader_*.bin`

脚本会自动查找并执行 `DB`。也可显式指定：

```bat
flash.bat boot -Loader firmware\MiniLoaderAll.bin
```

整包 `update.img` 不走这一步。

---

## 常用命令（纯 CLI）

`scripts\rk.bat` 透传 `upgrade_tool`：

```bat
scripts\rk.bat LD
scripts\rk.bat UF firmware\update.img
scripts\rk.bat UF firmware\update.img -noreset
scripts\rk.bat DI -boot firmware\boot.img
scripts\rk.bat DI -uboot firmware\uboot.img
scripts\rk.bat DI -rootfs firmware\rootfs.img
scripts\rk.bat EF firmware\update.img
scripts\rk.bat PL
scripts\rk.bat RD
```

| 命令 | 作用 |
|------|------|
| `LD` | 列出 Rockusb 设备 |
| `UF <固件>` | 升级整包固件（update.img） |
| `DI -<分区> <镜像>` | 下载分区镜像 |
| `DB <Loader>` | Maskrom 下 Download Boot |
| `UL <Loader>` | 写入 MiniLoader |
| `EF <Loader\|固件>` | 整片擦除 |
| `EL <起始> <扇区数>` | 按扇区擦除（eMMC） |
| `RCI` / `RFI` / `PL` | 芯片 ID / 存储信息 / 分区表 |
| `WL` / `RL` | 按 LBA 写 / 读 |
| `SSD` | 多存储切换 |
| `RD` / `RD 3` | 复位 / 切到 Maskrom |
| `-s <LocationID>` | 多设备选择 |

整包失败时推荐顺序：

```bat
flash.bat update -EraseFirst
```

或：

```bat
scripts\rk.bat EF firmware\update.img
scripts\rk.bat UF firmware\update.img
```

---

## 烧写前检查清单

- [ ] 已安装 DriverAssistant 驱动
- [ ] 设备管理器可见 Rockusb Device
- [ ] `flash.bat ld` 能列出设备（Mode=Loader 或 Maskrom）
- [ ] 镜像与芯片平台匹配（如 RK3588）
- [ ] 尽量使用数据线直连电脑 USB 口，避免劣质 Hub
- [ ] 建议以管理员权限打开终端
- [ ] 单分区 + Maskrom 时，`firmware\` 中已有 MiniLoader

---

## 和 GUI 的关系

| 工具 | 形态 | 用途 |
|------|------|------|
| `upgrade_tool.exe`（本包） | **命令行** | Windows 下脚本化烧写 |
| `RKDevTool.exe` | 图形界面 | 点选烧写，本包**不包含** |
| Linux `upgrade_tool` | 命令行 | 命令与本包基本一致（`uf` / `di` / `ld` 等） |

官方 `upgrade_tool` 为闭源二进制，本包只整理可执行文件与脚本，方便在 Windows 下走 CLI。

---

## 常见问题

**1. `No found any rockusb device`**  
驱动未装好，或板子未进 Loader/Maskrom。重装驱动后换线/换口再试。

**2. 烧写中途失败**  
整包可加 `-EraseFirst`；确认固件未损坏；避免休眠/断线。

**3. 烧写成功但不启动**  
确认镜像与芯片一致；检查启动存储介质（eMMC / SPI NAND 等）是否与固件匹配。

**4. Maskrom 下单烧 boot/rootfs 报错**  
缺少 MiniLoader。把 `MiniLoaderAll.bin` 放到 `firmware\`，或改烧整包 `update.img`。

**5. 需要批量自动化**

```bat
flash.bat update -SkipConfirm
flash.bat boot -SkipConfirm
```

---

## 版本信息

- `upgrade_tool`：**v2.23**（2023/08/28）
- DriverAssistant：**v5.12**
- 官方附带 PDF：`tools\upgrade_tool\upgrade_tool_manual.pdf`

如需换成你芯片 SDK 自带的更新版 `upgrade_tool.exe`，用新文件覆盖 `tools\upgrade_tool\` 下同名文件即可，脚本路径不用改。
