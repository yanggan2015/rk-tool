# Rockchip Windows CLI 升级工具包

在 Windows 下用命令行烧写瑞芯微 `update.img`，**不依赖 RKDevTool GUI**。

## 目录说明

```text
rk-tool/
├── 说明书.md                 ← 本说明书
├── firmware/                 ← 放置 update.img
├── scripts/
│   ├── flash-update.bat      ← 一键烧写（推荐）
│   ├── flash-update.ps1      ← PowerShell 版（支持更多参数）
│   ├── list-device.bat        ← 列出已连接设备
│   └── rk.bat                ← 透传任意 upgrade_tool 命令
└── tools/
    ├── upgrade_tool/         ← 官方 Windows CLI（v2.23）
    │   ├── upgrade_tool.exe
    │   ├── config.ini
    │   └── upgrade_tool_manual.pdf
    └── DriverAssistant/      ← USB 驱动安装器 v5.12
        └── DriverInstall.exe
```

工具来源：Vicharak 公开整理的 Rockchip 工具集  
`https://github.com/vicharak-in/rockchip-linux-tools`（`upgrade_tool_v2.23` / `DriverAssitant_v5.12`）

---

## 第一次使用：安装驱动

1. 以**管理员身份**运行：

   `tools\DriverAssistant\DriverInstall.exe`

2. 建议先点「驱动卸载」，再点「驱动安装」。

3. 用 USB 连接板子，让设备进入 **Loader** 或 **Maskrom** 模式。  
   设备管理器中应出现类似 **Rockusb Device** 的设备。

> 不同板子进模式方式不同：常见是按住 Recovery / Maskrom 键再上电，或以厂商文档为准。

---

## 最快上手：烧写 update.img

1. 把固件复制为：

   `firmware\update.img`

2. 双击或在终端运行：

   ```bat
   scripts\flash-update.bat
   ```

3. 看到设备列表后，输入 `Y` 确认，等待 `Upgrade firmware ok`。

也可直接指定路径：

```bat
scripts\flash-update.bat D:\images\rk3588_update.img
```

PowerShell 版（可选先擦除、烧完不重启）：

```powershell
cd C:\code\rk-tool
.\scripts\flash-update.ps1 -Image .\firmware\update.img
.\scripts\flash-update.ps1 -Image .\firmware\update.img -EraseFirst
.\scripts\flash-update.ps1 -Image .\firmware\update.img -NoReset -SkipConfirm
```

---

## 常用命令（纯 CLI）

在 `scripts` 目录可用 `rk.bat` 透传：

```bat
scripts\rk.bat LD
scripts\rk.bat UF C:\path\to\update.img
scripts\rk.bat UF C:\path\to\update.img -noreset
scripts\rk.bat EF C:\path\to\update.img
scripts\rk.bat RD
```

或进入工具目录直接调用：

```bat
cd tools\upgrade_tool
upgrade_tool.exe LD
upgrade_tool.exe UF ..\..\firmware\update.img
```

| 命令 | 作用 |
|------|------|
| `LD` | 列出 Rockusb 设备 |
| `UF <固件>` | 升级整包固件（update.img） |
| `UF <固件> -noreset` | 升级后不自动重启 |
| `EF <固件或Loader>` | 擦除 Flash（失败重试时常先擦再烧） |
| `SD` | 切换设备模式 |
| `RD` | 复位/重启设备 |
| `H` | 查看帮助 |
| `V` | 查看版本（无设备时可能报错，属正常） |

失败时推荐顺序：

```bat
upgrade_tool.exe EF update.img
upgrade_tool.exe UF update.img
```

---

## 烧写前检查清单

- [ ] 已安装 DriverAssistant 驱动  
- [ ] 设备管理器可见 Rockusb Device  
- [ ] `scripts\list-device.bat` 能列出设备（Mode=Loader 或 Maskrom）  
- [ ] `update.img` 与芯片平台匹配（如 RK3568 / RK3576 / RK3588）  
- [ ] 尽量使用数据线直连电脑 USB 口，避免劣质 Hub  
- [ ] 建议以管理员权限打开终端  

---

## 和 GUI 的关系

| 工具 | 形态 | 用途 |
|------|------|------|
| `upgrade_tool.exe`（本包） | **命令行** | Windows 下脚本化烧写 `update.img` |
| `RKDevTool.exe` | 图形界面 | 点选烧写，本包**不包含** |
| Linux `upgrade_tool` | 命令行 | 命令与本包基本一致（`uf` / `ld` 等） |

官方 `upgrade_tool` 为闭源二进制，本包只整理可执行文件与脚本，方便在 Windows 下走 CLI。

---

## 常见问题

**1. `No found any rockusb device`**  
驱动未装好，或板子未进 Loader/Maskrom。重装驱动后换线/换口再试。

**2. 烧写中途失败**  
先 `EF` 再 `UF`；确认固件未损坏；避免休眠/断线。

**3. 烧写成功但不启动**  
确认镜像与芯片一致；检查启动存储介质（eMMC / SPI NAND 等）是否与固件匹配。

**4. 需要批量自动化**  
用 PowerShell：

```powershell
.\scripts\flash-update.ps1 -Image .\firmware\update.img -SkipConfirm
```

或在自己的脚本里直接调用：

```bat
tools\upgrade_tool\upgrade_tool.exe UF firmware\update.img
```

---

## 版本信息

- `upgrade_tool`：**v2.23**（2023/08/28）  
- DriverAssistant：**v5.12**  
- 官方附带 PDF：`tools\upgrade_tool\upgrade_tool_manual.pdf`

如需换成你芯片 SDK 自带的更新版 `upgrade_tool.exe`，用新文件覆盖 `tools\upgrade_tool\` 下同名文件即可，脚本路径不用改。
